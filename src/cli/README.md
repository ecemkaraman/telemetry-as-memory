# `src/cli/` – Runners & Entry Points

This folder contains command-line interfaces for running experiments and evaluations.

- **`run_eval.py`** → Main entry point; runs experiments comparing baseline vs closed-loop learners, handles telemetry streaming, evaluation loop, and result logging.
- **`__init__.py`** → Package init.
